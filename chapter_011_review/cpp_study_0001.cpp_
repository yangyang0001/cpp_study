//
// Created by 杨建伟 on 2021/4/10.
//

# include <iostream>
# include <climits>
# include <string>
# include <cstring>
# include <vector>
# include <array>
# include <ctime>
# include <cctype>
# include <cmath>
# include <fstream>
# include "cpp_study_0001.h_"

using namespace std;

int main() {
    cout.setf(ios_base::fixed, ios_base::floatfield);

    Time time_a = Time(1, 15);
    Time time_b = Time(2, 45);
    cout << "0 ----------------------------------------------------------------------------------" << endl;

    Time sum = time_a + time_b;     // 4小时 0分钟
    cout << "1 ----------------------------------------------------------------------------------" << endl;

    Time sub = time_a - time_b;     // 1小时 30分钟
    cout << "2 ----------------------------------------------------------------------------------" << endl;

    Time pro = time_a * 5;          // 6小时 15分钟
    cout << "3 ----------------------------------------------------------------------------------" << endl;

    Time fri = 5 * time_b;          // 13小时 45分钟
    cout << "4 ----------------------------------------------------------------------------------" << endl;
    // 对象过期, 回收 fri, pro, sub, sum, time_b, time_a

    return 0;
}


