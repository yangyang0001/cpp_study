//
// Created by 杨建伟 on 2021/2/11.
//

# include <iostream>
# include <climits>
# include <string>
# include <cstring>

using namespace std;

int main() {
    cout.setf(ios_base::fixed, ios_base::floatfield);

    int number = 6;
    double cups = 4.5;

    cout << "number address is :" << &number << endl;
    cout << "cups   address is :" << &cups   << endl;
    cout << sizeof(double) << endl;

    return 0;
}

