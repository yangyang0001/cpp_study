//
// Created by 杨建伟 on 2021/2/11.
//

#include <iostream>
#include <climits>
#include <string>
#include <cstring>

using namespace std;

int main() {

    cout.setf(ios_base::fixed, ios_base::floatfield);

    enum color_enum {red, black};

    cout << "red   is :" << red << endl;
    cout << "black is :" << black << endl;

    return 0;
}

