//
// Created by 杨建伟 on 2021/1/18.
//

#include <iostream>

using namespace std;

double getYuanFromFen(double fen) {
    return fen / 100;
}

int main() {
    double fen = 0;
    cin >> fen;
    double yuan = getYuanFromFen(fen);
    cout << "I have " << yuan << " 元";
    return 0;
}
